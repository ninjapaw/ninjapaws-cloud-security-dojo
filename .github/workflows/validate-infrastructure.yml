name: Validate deployment assets

on:
  pull_request:
    paths:
      - 'infra/**'
      - 'scripts/**'
      - '.github/workflows/**'
      - 'Dockerfile'
  push:
    branches: [main, dev]
    paths:
      - 'infra/**'
      - 'scripts/**'
      - '.github/workflows/**'
      - 'Dockerfile'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Bash scripts
        run: |
          bash scripts/test.sh --skip-report
          bash -n scripts/setup-azure-github-oidc.sh

      - name: Compile Bicep
        run: |
          az bicep build --file infra/main.bicep --outfile /tmp/main.bicep.json

      - name: Validate JSON templates
        run: node -e "for (const file of ['infra/main.json']) JSON.parse(require('fs').readFileSync(file, 'utf8'));"