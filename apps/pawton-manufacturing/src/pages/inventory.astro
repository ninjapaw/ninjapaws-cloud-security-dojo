---
import Layout from '../layouts/Layout.astro';
import { isDatabaseConfigured, query } from '../lib/db.mjs';

let rows = [];
let columns = [];
let error = null;
if (isDatabaseConfigured()) {
  try {
    rows = await query('SELECT TOP 25 * FROM vw_InventoryValuation');
    columns = rows.length ? Object.keys(rows[0]) : [];
  } catch (err) {
    error = err.message;
  }
} else {
  error = 'Database is not configured on this Web App yet.';
}
---
<Layout title="Inventory">
  <h2>Inventory valuation</h2>
  <p>Current inventory value by warehouse and item type, from the <code>vw_InventoryValuation</code> report view.</p>
  {error && <div class="warn">{error}</div>}
  {!error && rows.length === 0 && <p>No inventory rows returned.</p>}
  {!error && rows.length > 0 && (
    <table>
      <thead><tr>{columns.map((c) => <th>{c}</th>)}</tr></thead>
      <tbody>
        {rows.map((row) => (
          <tr>{columns.map((c) => <td>{String(row[c] ?? '')}</td>)}</tr>
        ))}
      </tbody>
    </table>
  )}
</Layout>
