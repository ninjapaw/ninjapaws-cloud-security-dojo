services:
  dojo:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        UBUNTU_VERSION: ${UBUNTU_VERSION:?Run scripts/compose.sh so values load from config.json}
        NGINX_VERSION: ${NGINX_VERSION:?Run scripts/compose.sh so values load from config.json}
        NODE_MAJOR_VERSION: ${NODE_MAJOR_VERSION:?Run scripts/compose.sh so values load from config.json}
        VULNERABILITY_STATUS: ${VULNERABILITY_STATUS:?Run scripts/compose.sh so values load from config.json}
        PORT: ${PORT:?Run scripts/compose.sh so values load from config.json}
    ports:
      - "8080:80"
      - "3000:3000"
    environment:
      NGINX_VERSION: ${NGINX_VERSION:?Run scripts/compose.sh so values load from config.json}
      VULNERABILITY_STATUS: ${VULNERABILITY_STATUS:?Run scripts/compose.sh so values load from config.json}
      PORT: ${PORT:?Run scripts/compose.sh so values load from config.json}
      DEFENDER_ENABLED: ${DEFENDER_ENABLED:?Run scripts/compose.sh so values load from config.json}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./app.js:/app/app.js:ro
